namespace Phantasma.Business.Tests.CodeGen.Assembler;

using Xunit;

using Phantasma.Business.CodeGen.Assembler;

public class ArgumentUtilsTests
{
    // write ArgumentUtilsTests based on the class ArgumentUtils and on the methods of the class
    [Fact]
    public void TestMethod1()
    {
        /*// arrange
        // create a byte with the value 0x01
        byte b = 0x01;
        var alias = "test1,";
        
        ArgumentUtils.RegisterAlias(alias, b);
        
        // check if the test1 alias is registered
        
        // check if the alias is equal to the byte
        Assert.True(alias.AsRegister() == b);
        
        // assert*/
    }
}
